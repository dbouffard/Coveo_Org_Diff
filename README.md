# coveo-org-diff

Compares two Coveo organization JSON exports.

## Usage

1. Open `index.html` in a browser.
2. Upload the left and right organization JSON files.
3. Review the generated differences.
